# Dependencies are declared in pyproject.toml (the single source of truth).
# `pip install -r requirements.txt` installs the dsl_topic package together with
# all of its pinned dependencies, exactly like `pip install -e .`.
-e .
