"""Vendored third-party topic-model implementations (kept intact for fidelity).

These are external baselines adapted by DSL only at the edges (the authors' own
DSL models live in ``dsl_topic.models.dsl``). See ``NOTICE.md`` for attribution:
  - octis/    : ProdLDA, CombinedTM/ZeroShotTM (CTM), ETM, LDA  (OCTIS)
  - topmost/  : ECRTM                                            (TopMost)
  - fastopic/ : FASTopic                                         (FASTopic)
"""
